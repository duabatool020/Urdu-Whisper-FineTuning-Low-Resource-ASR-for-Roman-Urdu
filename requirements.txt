# Dependencies for Whisper Urdu → Roman Urdu ASR fine-tuning
# Tested on Google Colab (Python 3.12, CUDA-enabled GPU)

# Core ML
torch>=2.1.0
torchaudio>=2.1.0
numpy>=1.24.0

# HuggingFace ecosystem
transformers>=4.35.0
datasets>=2.14.0
evaluate>=0.4.0
accelerate>=0.24.0
huggingface-hub>=0.19.0
tokenizers>=0.14.0
safetensors>=0.4.0

# Speech / audio processing
librosa>=0.10.0
soundfile>=0.12.1
audioread>=3.0.0
resampy>=0.4.2
soxr>=0.3.5

# Evaluation metrics
jiwer>=3.0.0
scikit-learn>=1.3.0

# Data utilities (used by datasets internally)
pyarrow>=12.0.0
multiprocess>=0.70.14
fsspec>=2023.6.0
xxhash>=3.3.0

# Logging (enabled via Trainer)
tensorboard>=2.14.0
tqdm>=4.65.0
packaging>=23.0
pyyaml>=6.0
regex>=2023.0.0
requests>=2.31.0
filelock>=3.12.0